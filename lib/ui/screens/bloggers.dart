import 'package:flutter/material.dart';

class BloggersScreen extends StatelessWidget {
  const BloggersScreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Bloggers')));
  }
}
