import 'package:flutter/material.dart';

class PressToursScreen extends StatelessWidget {
  const PressToursScreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Press Tours')));
  }
}
